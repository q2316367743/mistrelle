appId: com.electron.app
productName: mistrelle
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  - '!{.claude/**,.githooks/**,.github/**}'
  - '!docs/**'
  - '!{.claude/**,.githooks/**,.github/**,.zcode/**}'
  - '!docs/**'
  # ffmpeg 二进制经下方 extraResources 按平台注入（asar 之外），勿再进 asar 双份
  - '!resources/ffmpeg/**'
asarUnpack:
  - resources/**
  - '**/node_modules/sharp/**/*'
# 每个平台的安装包只带自己的 ffmpeg，落在 process.resourcesPath/ffmpeg/（可直接 spawn）
# 打包前须先跑 node scripts/fetch-ffmpeg.mjs，否则源目录缺失构建报错
extraResources:
  - from: 'resources/ffmpeg/${os}-${arch}'
    to: ffmpeg
win:
  executableName: mistrelle
  artifactName: ${name}-${version}-${os}-${arch}.${ext}
  target:
    - target: nsis
      arch:
        - x64
    - target: zip
      arch:
        - x64
  # 注册 mistrelle:// 系统级链接（win）
  protocols:
    - name: mistrelle
      schemes:
        - mistrelle
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  oneClick: false
  allowToChangeInstallationDirectory: true
  perMachine: false
  deleteAppDataOnUninstall: false
mac:
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
    # 注册 mistrelle:// 系统级链接（macOS，CFBundleURLTypes）
    - CFBundleURLTypes:
        - CFBundleURLName: mistrelle
          CFBundleURLSchemes:
            - mistrelle
  notarize: false
  target:
    - dmg
    - zip
  artifactName: ${name}-${version}-${os}-${arch}.${ext}
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: xyz.esion
  category: Utility
  # 注册 mistrelle:// 系统级链接（linux）
  protocols:
    - name: mistrelle
      schemes:
        - mistrelle
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: generic
  url: https://example.com/auto-updates
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
